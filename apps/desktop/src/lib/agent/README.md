# lib/agent

ACP 辅助与 Agent Markdown。文件说明见 [../README.md](../README.md) 的 **agent/** 节。

Inkdown 注入 Agent 的 Skill / 快照 / registry 在 [`context/`](./context/)。

ACP 传输/认证/会话/MCP 纯逻辑见 `@inkdown/acp`（`packages/acp/`）；标记提议解析与 promote 见上级 README agent/ 节（模型在 `@inkdown/annotations`：`mark-proposal` / `chapter-mark-plan`）。

增删或改名后，更新上级 README。
