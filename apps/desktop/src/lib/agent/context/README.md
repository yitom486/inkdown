# lib/agent/context

Inkdown → Agent 上下文：静态 Skill、turn-context、阅读器内容/选区/标记 registry、MCP 快照序列化。

说明见 [../../README.md](../../README.md) 的 **agent/** 节（`agent/context/`）。

MCP 工具表与传输纯逻辑见 `@inkdown/acp`（`packages/acp/`）；快照/虚拟文件类型见 `@inkdown/contracts`（`packages/contracts/src/agent/`）。

增删或改名后，更新 `apps/desktop/src/lib/README.md`。
